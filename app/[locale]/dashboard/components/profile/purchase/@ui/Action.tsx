"use client";
type PurchaseActionProps = {
}

export const PurchaseAction = ({}: PurchaseActionProps) => {
  return (
      <div>
          If user has purchase this product, show this component for the user to take action (e.g. download, etc.)
    </div>
  )
}
